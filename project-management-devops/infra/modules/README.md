# Terraform Modules

This directory is intentionally reserved for module extraction as the platform evolves.

Current implementation keeps resources in a single stack for readability.
Recommended next step is extracting:

- `networking`
- `compute`
- `database`
- `edge`
- `observability`